<%= form_for(@admin_feature) do |f| %>
  <% if @admin_feature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_feature.errors.count, "error") %> prohibited this admin_feature from being saved:</h2>

      <ul>
      <% @admin_feature.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
