<%= form_for(@admin_property_type) do |f| %>
  <% if @admin_property_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_property_type.errors.count, "error") %> prohibited this admin_property_type from being saved:</h2>

      <ul>
      <% @admin_property_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
